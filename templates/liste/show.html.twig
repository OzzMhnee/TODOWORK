{% extends 'base.html.twig' %}

{% block body %}
<div class="max-w-lg mx-auto mt-10 bg-white p-8 rounded shadow">
    <h1 class="text-2xl font-bold mb-6 text-blue-400">{{ liste.name }}</h1>
    <p class="mb-2"><strong>Position :</strong> {{ liste.position }}</p>
    <p class="mb-2"><strong>Board :</strong> {{ liste.board ? liste.board.name : 'Aucun' }}</p>
    <a href="{{ path('app_liste_edit', {'id': liste.id}) }}" class="text-green-600 hover:underline mr-4">Modifier</a>
    <a href="{{ path('app_liste') }}" class="text-blue-400 hover:underline">Retour à la liste</a>
</div>
{% endblock %}
